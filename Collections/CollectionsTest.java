public class CollectionsTest {
    public static void main(String[] args) {
        // Create an instance of your Collections class where the code is written
        Collections collections = new Collections();

        // we call our methods on the instance
        collections.greet();
        collections.makeArray();
        collections.makeArrayList();
        collections.makeHashMap();
    }
}
